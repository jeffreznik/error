{
  "name": "@jeffreznik/error",
  "version": "0.1.0",
  "description": "A collection of typed error subclasses with additional type-specific properties",
  "main": "./lib/errors.js",
  "scripts": {
    "build": "rm -rf lib && babel src --out-dir lib",
    "prepublish": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jeff Reznik",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/jeffreznik/error.git"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-plugin-transform-builtin-extend": "^1.1.2",
    "babel-preset-es2015": "^6.24.1"
  }
}
